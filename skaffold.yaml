apiVersion: skaffold/v4beta6
kind: Config
metadata:
  name: token-server-demo
build:
  artifacts:
  - image: token-server-demo
    docker:
      dockerfile: Dockerfile
manifests:
  rawYaml:
  - k8s/deployment.yaml
  - k8s/service.yaml
  - k8s/gateway.yaml
